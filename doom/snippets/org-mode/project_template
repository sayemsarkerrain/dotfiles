# -*- mode: snippet -*-
# name: Project Template
# key: pro
# --

* PROJ [#${1:A|B|C}] ${2:Task}
$0
`(progn
   (add-hook 'yas-after-exit-snippet-hook
             (lambda ()
               (org-deadline nil (org-read-date nil nil nil "DEADLINE: ")))
             nil t)
   "")`